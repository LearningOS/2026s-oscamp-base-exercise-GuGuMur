# HINT

1. 先画出上下文结构和栈布局，确认保存/恢复寄存器顺序与结构体字段一致。
2. `TaskContext::init` 里初始化栈顶指针和首次返回地址（通常指向入口函数）。
3. `switch_context` 的裸函数汇编要遵守目标 ABI，并维持栈 16 字节对齐。
4. 切换流程是“保存当前上下文 -> 加载目标上下文 -> ret/jr 跳转”。
5. 常见错误：寄存器顺序错位、栈未对齐、naked 函数里混入 Rust 语句。
