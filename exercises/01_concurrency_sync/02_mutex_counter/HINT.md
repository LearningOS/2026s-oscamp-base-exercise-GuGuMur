# HINT

1. 共享计数器请用 `Arc<Mutex<T>>`，每个线程里先 `Arc::clone` 再 `lock()`。
2. `lock()` 返回 guard，修改数据应在 guard 生命周期内完成，离开作用域自动解锁。
3. 先收集所有 `JoinHandle`，最后统一 `join`，避免主线程提前结束。
4. 如果测试比较结果集合，注意线程结果可能乱序，必要时排序后比较。
5. 常见错误：持锁时间过长、在持锁期间做耗时操作、忘记处理 `PoisonError`。
