# NOTE

## 核心知识
- 线程是 OS 调度的基本执行单元，Rust 用 `std::thread` 提供安全封装。
- 闭包加 `move` 会把捕获变量所有权转移到线程，避免悬垂引用。
- `JoinHandle::join` 不仅等待结束，也能把子线程 panic 传回主线程。

## 拓展应用
- 可用于并行预处理、日志分流、批量 CPU 密集计算。
- 进一步可学习线程池（如 rayon）减少频繁创建线程的开销。
