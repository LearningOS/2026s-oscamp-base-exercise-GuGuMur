# HINT

1. 先看测试里要求的返回值顺序和线程数量，再决定你要不要在主线程里维护 `Vec<JoinHandle<_>>`。
2. 在线程闭包里如果要用外部变量，通常要加 `move`，把所有权搬进线程。
3. 对每个 `JoinHandle` 调用 `join()`，并处理 `Result`，避免测试里因 panic 导致失败。
4. 如果题目涉及线程命名或栈大小，使用 `thread::Builder` 而不是 `thread::spawn`。
5. 常见错误：忘记 `join`、闭包捕获借用导致生命周期不够、误以为线程执行顺序可预测。
