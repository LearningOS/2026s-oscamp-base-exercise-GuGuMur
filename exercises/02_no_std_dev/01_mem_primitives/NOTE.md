# NOTE

## 核心知识
- `no_std` 环境常需手写内存原语，底层依赖裸指针和 `unsafe`。
- `memcpy` 不保证重叠安全，`memmove` 专门处理重叠拷贝。
- C 字符串以 `\0` 结尾，`strlen/strcmp` 都基于这一约定。

## 拓展应用
- 内核、引导程序、嵌入式运行时常直接使用这些原语。
- 可进一步实现 `memcmp/strncpy` 并做边界与未定义行为测试。
