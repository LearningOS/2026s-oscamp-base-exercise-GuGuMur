# HINT

1. SV39 虚拟地址分成三级 VPN 索引和页内偏移，先写 `extract_vpn` 并用测试校验。
2. `map_page` 逐级下钻：中间节点不存在就分配，最后一级写叶子项。
3. `translate` 逐级遍历，遇到无效项直接 fault；遇到叶子项按页级别拼接物理地址。
4. 大页映射本质是在更高层直接放叶子项，偏移拼接规则要随级别变化。
5. 常见错误：三级索引顺序反了、叶子判断条件错误。
